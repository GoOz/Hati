<hr class="post-list__divider " />

<nav class="pagination" role="navigation">
    {{#if prev}}
        <a class="newer-posts pagination__newer btn btn-small btn-tertiary" href="{{page_url prev}}#blog">&larr; Billets plus récents</a>
    {{/if}}
    <span class="pagination__page-number">Page {{page}} sur {{pages}}</span>
    {{#if next}}
        <a class="older-posts pagination__older btn btn-small btn-tertiary" href="{{page_url next}}#blog">Billets plus anciens &rarr;</a>
    {{/if}}
</nav>
